# Base da API vista pelo browser. Em dev use /api (proxy do Vite). Em prod, mesma origem via reverse proxy.
VITE_API_URL=/api
# Origem do socket.io. Vazio = mesma origem (proxy /socket.io).
VITE_WS_URL=
# Usado só pelo proxy do Vite (Node), nunca vai para o bundle.
BACKEND_URL=...
